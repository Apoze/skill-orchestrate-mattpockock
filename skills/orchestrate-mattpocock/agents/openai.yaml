interface:
  display_name: "Orchestrate Matt Pocock"
  short_description: "Relay tickets through fresh Codex sessions"
  default_prompt: "Use $orchestrate-mattpocock to run the ready tickets sequentially in fresh Codex sessions."
policy:
  allow_implicit_invocation: false
