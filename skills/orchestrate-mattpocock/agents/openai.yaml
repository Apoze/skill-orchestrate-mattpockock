interface:
  display_name: "Orchestrate Matt Pocock"
  short_description: "Run Matt Pocock tickets in fresh threads"
  default_prompt: "Use $orchestrate-mattpocock to run the ready tickets sequentially in fresh Codex App threads."
policy:
  allow_implicit_invocation: false
